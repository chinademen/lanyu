import handleStorage from './lib/storage';
import { floatOperation, stampToDay, getFormatDate, timeFormat, objToStr, listAddKey, moneyFormat } from './lib/format';

export {
    // 数据存储
    handleStorage,
    // 格式化
    floatOperation, 
    stampToDay, 
    getFormatDate, 
    timeFormat, 
    objToStr, 
    listAddKey, 
    moneyFormat,
}